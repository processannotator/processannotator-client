{
  "name": "ProjectAnnotationd",
  "version": "0.0.1",
  "description": "Server application for ProjectAnnotator. Uses websockets to communicate",
  "dependencies": {
    "follow": "^0.12.1",
    "nano": "^6.2.0",
    "ws": "^1.0.1"
  },
  "scripts": {
    "start": "echo please start couchdb manually if not running. && node server.js"
  }
}
