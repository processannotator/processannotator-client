{
  "name": "DokuClient",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "pouchdb": "^5.3.2",
    "pouchdb-authentication": "^0.5"
  },
  "scripts": {
    "build": "babel ui.js --out-file bundle.js --source-maps",
    "install": "bower install",
    "start": "npm run build && electron .",
    "package": "npm run build && electron-packager . ProcessAnnotator --platform=darwin --arch=x64 --out=dist --overwrite"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "electron-packager": "^7.0",
    "electron-prebuilt": "^1.0.1"
  }
}
